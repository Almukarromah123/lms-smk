# Railway Production Environment Variables
# Copy these settings ke Railway dashboard untuk production

# Django Settings
DEBUG=False
ALLOWED_HOSTS=lms-smk-almukarromah.up.railway.app,www.lms-smk-almukarromah.up.railway.app

# Secret Key (gunakan yang aman di production, bukan development key!)
SECRET_KEY=your-production-secret-key-here

# PostgreSQL (dari Railway database)
DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# HTTPS/Security Settings - IMPORTANT FOR CAMERA FEATURE!
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# CSRF Trusted Origins
CSRF_TRUSTED_ORIGINS=https://lms-smk-almukarromah.up.railway.app,https://*.up.railway.app

# Optional: Email Configuration (untuk notifications)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
